# svr-admin 配置文件
# 优先级: 命令行参数 > 环境变量 > 配置文件 > 默认值
# 环境变量格式: SECTION_KEY (如 SERVER_PORT, DB_HOST)

server:
  port: 8080
  mode: debug    # debug / release / test

database:
  host: 127.0.0.1
  port: 3306
  user: root
  password: ""
  dbname: svr_admin
