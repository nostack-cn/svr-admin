# svr-admin 环境变量配置模板
# 复制为 .env 后按实际环境修改
# 优先级: 命令行参数 > 环境变量 > 配置文件(config.yaml) > 默认值

# 也可以通过 --config/-c 指定配置文件:
#   ./svr-admin -c /path/to/config.yaml

SERVER_PORT=8080
SERVER_MODE=debug

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=svr_admin
